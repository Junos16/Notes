# $\Theta(n^2)$ Sorting Algorithms
# $\Theta(nlogn)$ Sorting Algorithms
## Merge Sort
## Heap Sort
```
heap_sort(A,n):
	build_max_heap(A,n)
	for i = n to 2:
		temp = A[1] 
		A[i] = A[1]
		A[1] = temp
		A.heap_size = A.heap_size - 1
		max_heapify(A,1) 
```

## Proof of correctness
## Quick Sort
# $\Theta(n)$ Sorting Algorithms
# Selection
- 
